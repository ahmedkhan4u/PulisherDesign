<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.softrasol.ahmed.pulisherdesign">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SingleSelectionPubliserActivity"></activity>
        <activity android:name=".HomeActivity" />
        <activity android:name=".UploadPublicationActivity"></activity>
        <activity android:name=".PayoutMethodActivity" />
        <activity android:name=".ChangeBusinessInfoActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".OtpActivity" />
        <activity android:name=".ProfileUpdateActivity" />
        <activity android:name=".FaqActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".TermsAndConditionActivity" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUpActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>